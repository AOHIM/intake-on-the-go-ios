workflows:
  ios-workflow:
    name: Intake on-the-Go iOS
    max_build_duration: 60
    integrations:
      app_store_connect: Codemagic
    environment:
      xcode: latest
      cocoapods: default
      groups:
        - app_store_credentials
      ios_signing:
        distribution_type: app_store
        bundle_identifier: org.aohministry.intakeonthego
    scripts:
      - name: Unzip project
        script: |
          unzip "Intake on-the-Go.zip"
      - name: Install CocoaPods
        script: |
          cd src
          pod install
      - name: Set up code signing
        script: |
          cd src
          xcode-project use-profiles
      - name: Build iOS
        script: |
          cd src
          xcodebuild -workspace "Intake on-the-Go.xcworkspace" \
            -scheme "Intake on-the-Go" \
            -configuration Release \
            -archivePath build/Intake-on-the-Go.xcarchive \
            archive
    artifacts:
      - src/build/Intake-on-the-Go.xcarchive
